# North Star Pacific

React frontend for the North Star Pacific marketing site.

## Local Development

```bash
npm install
npm run dev
```

## Quality Checks

```bash
npm run lint
npm run build
```

## Deploy

The project is configured for Vercel with `vercel.json`. Vercel should use:

- Build command: `npm run build`
- Output directory: `dist`

